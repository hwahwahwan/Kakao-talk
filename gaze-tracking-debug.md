# 시선 추적 디버깅 컨텍스트

> 이 파일은 시선 추적이 안 되던 문제의 전체 흐름을 기록한 것입니다.

---

## 증상

- gaze-server는 "연결됨" 상태인데 카테고리 선택이 전혀 안 됨
- 빨간 디버그 점이 시선을 따라오지 않고 제멋대로 움직임
- y 좌표가 항상 0 (화면 최상단에 고정)

---

## 원인 분석

### 1. 좌우 반전 버그 (처음 발견)
- GazeTracking 라이브러리의 `horizontal_ratio()` 는 `0=오른쪽, 1=왼쪽` (화면 x와 반대)
- 기존 코드: `x = norm(h) * W` → 좌우 반전됨
- 수정: `x = (1 - norm(h)) * W`

### 2. 보정 상수가 계속 틀림 (핵심 문제)
- `gazeConfig.ts`에 하드코딩된 `GAZE_H_MIN/MAX`, `GAZE_V_MIN/MAX` 값이 이 사람의 실제 눈 움직임 범위와 맞지 않음
- 스크린샷 데이터로 역산하려 했으나 매번 틀렸음

#### 시도 1: 기본 범위 0.3~0.7
- 결과: 점이 화면 가장자리에만 붙음

#### 시도 2: 스크린샷 2장(영화/게임 버튼)에서 역산
```
h=0.640 → x≈666 (영화)
h=0.624 → x≈978 (게임)
→ 계산된 수평 범위: H_MIN=0.59, H_MAX=0.67 (폭 0.08)
→ 수직: V_MIN=0.57, V_MAX=0.87
```
- **실패 이유**: V_MIN=0.57이 실제 사용자의 v값(0.519~0.529)보다 높아서 수직이 전부 0으로 clamp됨
- 결과: y 항상 0 → 점이 화면 최상단에 고정, 버튼 히트 불가능

#### 시도 3: 더 틀린 이유
- 스크린샷에서 관측한 v값(0.595~0.726)과 실제 측정값(0.519~0.529)이 달랐음
- 앉은 자세, 조명, 머리 위치에 따라 매 세션마다 비율이 달라짐
- **즉, 하드코딩 상수는 근본적으로 불안정**

---

## 실제 해결 방법

### 인앱 보정 UI 구현 (`CalibrationOverlay.tsx`)

상수를 추측하는 대신, 사용자가 직접 4개 코너를 바라보면 자동으로 이 사람의 범위를 측정하여 localStorage에 저장.

```
보정 흐름:
좌상단 2.5초 → 우상단 2.5초 → 우하단 2.5초 → 좌하단 2.5초
→ 각 지점의 h/v 평균값 기록
→ H_MIN = min(우상단.h, 우하단.h)
→ H_MAX = max(좌상단.h, 좌하단.h)
→ V_MIN = min(좌상단.v, 우상단.v)
→ V_MAX = max(우하단.v, 좌하단.v)
→ calibrationStorage.ts → localStorage 저장
```

### 관련 파일 변경 목록

| 파일 | 변경 내용 |
|------|-----------|
| `client/src/utils/calibrationStorage.ts` | 신규 — localStorage 저장/로드 |
| `client/src/components/CalibrationOverlay.tsx` | 신규 — 4점 보정 UI |
| `client/src/utils/gazeUtils.ts` | 보정값 우선 사용, 없으면 gazeConfig 폴백 |
| `client/src/constants/gazeConfig.ts` | 폴백 상수를 넓게 설정 (clamp 방지) |
| `client/src/pages/CategoryPage.tsx` | "보정 필요/재보정" 버튼 + CalibrationOverlay 연결 |
| `client/src/hooks/useCategoryGaze.ts` | 이동 평균 스무딩 추가 (SMOOTH_N=15 프레임) |

---

## 교훈

1. 눈 추적 비율 범위는 사람마다, 세션마다 다름 → 하드코딩 불가
2. 스크린샷에서 상수를 역산하는 것은 불안정 → 반드시 런타임 보정 필요
3. y 좌표 clamp 문제: V_MIN이 실제 사용자 값보다 높으면 y=0 고정됨
4. 보정 전 폴백 상수는 "정확함"보다 "clamp 안 됨"을 우선해야 함

---

## 현재 상태 (2026-04-24)

- 보정 UI 구현 완료
- 앱 실행 후 헤더의 **"보정 필요"** 버튼으로 보정 시작
- 보정 완료 시 localStorage에 저장, 재시작해도 유지
- **아직 실제 보정을 실행하여 동작 확인은 안 됨 → 다음 세션에서 확인 필요**

# KakaoTalk Desktop Web 구현 - 디자인 분석

## 1. 전체 레이아웃 구조

```
┌──────────────────────────────────────────────┐
│  사이드바  │        메인 콘텐츠 영역           │
│  (72px)   │                                  │
│           │  헤더 (탭/타이틀)                 │
│  아이콘   │  ─────────────────────────────── │
│  메뉴     │  콘텐츠 리스트 / 채팅창           │
│           │                                  │
│  (하단)   │                                  │
│  알림     │                                  │
│  설정     │                                  │
└──────────────────────────────────────────────┘
```

---

## 2. 색상 시스템

| 역할 | 색상 | 비고 |
|---|---|---|
| 카카오 브랜드 | `#FEE500` | 내 말풍선, 배지, 강조 |
| 사이드바 배경 | `#F9F9F9` | 연한 회색 |
| 채팅방 배경 | `#B2C7D9` | 연한 파란 회색 (기본) |
| 받은 말풍선 | `#FFFFFF` | 흰색 |
| 보낸 말풍선 | `#FEE500` | 카카오 옐로우 |
| 텍스트 주 | `#1A1A1A` | 거의 검정 |
| 텍스트 보조 | `#888888` | 회색 |
| 헤더/패널 배경 | `#FFFFFF` | 흰색 |
| 구분선 | `#EBEBEB` | 연한 회색 |
| 미읽음 배지 | `#FE4141` | 빨간색 (오픈채팅 등) |
| 알림 빨간 점 | `#FE4141` | 백업 메뉴 등 |

---

## 3. 사이드바 (Left Navigation)

### 구조
- 너비: 약 72px
- 상단: 내 프로필 아이콘 (원형 아바타)
- 중단: 채팅 아이콘 + `999+` 배지 (빨간 배경)
- 중단: 더보기 `···` 아이콘
- 하단: 알림(벨) 아이콘
- 하단: 설정(기어) 아이콘

### 아이콘 상태
- 활성 탭: 진한 색
- 비활성 탭: 회색
- 배지: 빨간 원형, 흰색 숫자 텍스트

---

## 4. 친구 탭 (Friends)

### 헤더
- 타이틀: "친구" (bold, 18px)
- 우측 아이콘: 검색(돋보기), 친구 추가(사람+)

### 리스트 아이템 구조
```
[아바타 40px] [이름 (bold)]
              [상태 메시지 (gray, 13px)]
```
- 아이템 높이: 약 60px
- 아바타: 원형, 회색 기본 / 프로필 사진
- 패딩: 좌우 16px, 상하 10px
- 일부 아이템은 상태 메시지만 표시 (이름 없이 `.` 로 표기)

---

## 5. 채팅 탭 (Chats)

### 헤더
- "채팅" 텍스트 + 파란 점 인디케이터
- "오픈채팅" 탭 전환 버튼
- 우측: 검색, 채팅 작성 아이콘

### 필터 탭 바
- 스크롤 가능한 수평 칩(Chip) 형태
- 항목: `전체` | `안읽음 430` | `😊` | `School 9` | `P...`
- 활성 칩: 진한 배경 (검정), 흰 텍스트
- 비활성 칩: 연한 회색 배경, 어두운 텍스트
- 숫자 배지는 칩 내부에 표시

### 채팅 리스트 아이템
```
[아바타]  [이름 (bold 14px)]          [시간 (gray 12px)]
          [마지막 메시지 (gray 13px)]  [미읽음 배지]
```
- 아바타: 40px 원형 (그룹 채팅은 2x2 레이아웃)
- 미읽음 배지: 빨간 원형, 숫자
- 특수 아이콘: 채팅방 이름 좌측에 채널 아이콘 (K-패스 등)
- 첨부 파일 표시: "파일: " 접두사

---

## 6. 더보기 탭 (More)

### 프로필 카드
- 배경: 연한 회색 카드
- 좌측: 이름 (bold), 이메일 (gray)
- 우측: 보안 방패 아이콘 (파란색+노란색)

### 서비스 그리드
- 4열 그리드
- 각 항목: 원형/사각 아이콘 + 텍스트 라벨
- 항목 목록:
  - 다음, 메일, 캘린더, 톡클라우드
  - 이모티콘, 선물하기, 톡딜, 실험실
  - 내 결제, 공지사항(빨간 점), 환경설정, 도움말

### 하단
- 카카오톡 정보 (ℹ️ 아이콘)
- 업데이트 버튼 (테두리 스타일)

---

## 7. 채팅창 (Chat Window)

### 헤더
- 좌측: 원형 아바타 + 대화 상대 이름 (bold) + 참여자 수 (gray)
- 우측 아이콘: 검색, 전화, 화상통화, 메뉴(≡)

### 메시지 영역
- 배경: `#B2C7D9` (연한 청회색)
- **받은 메시지**: 좌측 정렬, 흰색 말풍선, 아바타 없음(1:1) 또는 있음(단체)
- **보낸 메시지**: 우측 정렬, 카카오 노랑 말풍선
- 말풍선 radius: 18px (한쪽 모서리 뾰족)
- 시간: 말풍선 옆 아래 (gray, 11px)
- 날짜 구분선: 중앙 정렬, 회색 배경 pill 형태 + 📅 아이콘

### 스크롤 인디케이터
- 우측 끝에 날짜 표시 고정 pill (예: "3. 18. 수")

### 입력창
- 배경: 흰색
- 좌측 버튼: `+` (파일/미디어 첨부), 😊 (이모티콘), 📄 (파일)
- 텍스트 입력: 가운데, placeholder "메시지 입력"
- 우측: `전송` 버튼 (카카오 노랑, radius 8px) + 드롭다운 화살표

---

## 8. 설정 창 (Settings)

### 레이아웃
```
┌─────────────┬──────────────────────────────┐
│  설정       │ 헤더 타이틀                   │
│  사이드 메뉴│                              │
│  계정       │  설정 콘텐츠 영역             │
│  보안       │                              │
│  알림       │                              │
│  친구       │                              │
│  채팅       │                              │
│  백업 🔴   │                              │
│  이모티콘   │                              │
│  화면       │                              │
│  통화       │                              │
│  실험실     │                              │
│  저장공간   │                              │
└─────────────┴──────────────────────────────┘
```
- 설정 창 크기: 약 600x480px
- 왼쪽 사이드 너비: 약 140px
- 활성 메뉴: 배경 회색 강조
- 빨간 점: 백업 메뉴에 표시 (알림)

### 계정 탭
- 섹션: 계정 정보 (회색 카드), 기본프로필, 멀티프로필
- 계정 카드: 이메일, ID (회색 배경 rounded)
- 프로필: 아바타 + 이름 + 친구 수 + [편집] [친구관리] 버튼
- 멀티프로필: (0/3) 카운터, 설명 텍스트, + 버튼

### 보안 탭
- 라디오 버튼 그룹: 자동 실행 & 자동 로그인
- 체크박스 + ⓘ 아이콘
- 버튼: "PC 인증 해제" (테두리 스타일)

### 알림 탭
- 라디오: 소리 알림
- 현재 알림음 + [알림음 설정] 버튼
- 체크박스: 알림창 + 드롭다운 (보낸사람 + 메시지)
- 라디오: 키워드 알림 + [키워드 편집] 버튼 (비활성)
- 라디오: 답장/댓글 알림
- 체크박스: 멘션 알림 + 드롭다운 (모든 채팅방)
- 체크박스: 보이스룸 알림

### 친구 탭
- 동기화 버튼 + 마지막 동기화 시간
- [목록 보기] 버튼
- 체크박스 그룹: 생일인 친구 / 업데이트한 프로필 / 기억할 친구

---

## 9. 타이포그래피

| 요소 | 크기 | 굵기 |
|---|---|---|
| 섹션 타이틀 | 18px | Bold |
| 이름 (리스트) | 14px | SemiBold |
| 상태 메시지 | 13px | Regular |
| 메시지 시간 | 11-12px | Regular |
| 말풍선 텍스트 | 14px | Regular |
| 설정 메뉴 | 14px | Regular |
| 배지 숫자 | 11px | Bold |

---

## 10. 컴포넌트 목록

- `Sidebar` - 좌측 고정 네비게이션
- `FriendList` - 친구 목록 탭
- `ChatList` - 채팅방 목록 탭
- `FilterChips` - 채팅 탭 필터 칩 바
- `MoreTab` - 더보기 탭 (서비스 그리드)
- `ChatWindow` - 채팅창 (헤더 + 메시지 + 입력창)
- `MessageBubble` - 말풍선 (sent/received)
- `DateDivider` - 날짜 구분선
- `MessageInput` - 메시지 입력창
- `SettingsModal` - 설정 모달
- `SettingsSidebar` - 설정 좌측 메뉴
- `Avatar` - 원형 프로필 이미지
- `Badge` - 미읽음 카운트 배지

---

## 11. 기술 스택 제안

- **Framework**: React + TypeScript
- **Styling**: Tailwind CSS (또는 CSS Modules)
- **State**: Zustand (가벼운 전역 상태)
- **라우팅**: React Router (탭 전환)
- **아이콘**: react-icons 또는 커스텀 SVG

---

## 12. 화면 구현 우선순위

1. `Sidebar` + `FriendList` (기본 레이아웃)
2. `ChatList` + `FilterChips`
3. `ChatWindow` + `MessageBubble` + `MessageInput`
4. `MoreTab`
5. `SettingsModal`
